<html>
  <head>
    <title>The Gossip Project</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  </head>
  <body>
  <section class="container"> 
    <h1>Mon super site de gossip, depuis le dossier views !</h1>
   <% gossips.each do |gossip| %>
        <p> Voici le potin n° <%= gossip.id%><br/>
        Son auteur : <%= gossip.author %><br/>
        Le contenu du potin : <%= gossip.content %>
        <a class='btn btn-success' href="<%= gossip.id%>/edit">editer le potin</a>
        <form action="<%=gossip.id%>/delete" method="POST">
    <output name="delete"><%id%></output>
    <input class='btn btn-danger' type="submit" value="Delete">
    </form>
        </form>

        </p>
    <% end %>
    </section>
  </body>
</html>